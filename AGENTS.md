# 3D Viewer Repo Notes

- This is the temporary public standalone version of Ryan's local 3D viewer.
- Keep this repo independent from the AutoHotkey implementation.
- Do not add Meshy, Generated Asset, Ryan machine hotkeys, or personal runtime state here.
- The app icon is the public `rIcon` in `assets/`.
- Keep HDRIs optional and downloadable, not committed, so the repo stays small. The bundled sample model must keep working offline.
